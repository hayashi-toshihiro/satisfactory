<header>
  <div class= "container">
    <div class="title_header">
      <p>Satisfactory create</p>
    </div>
    <% flash.each do |message_type, message| %>
      <div class="alert alert-<%= message_type %>"><%= message %></div>
    <% end %>
  </div>
</header>